#!/usr/bin/env python
"""
Setup.py fallback for editable installs.

This file exists primarily to support `pip install -e .` for development.
The main configuration is in pyproject.toml.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
